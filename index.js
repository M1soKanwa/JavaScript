const user = {
    firstName: 'John',
    lastName: 'Doe',
    age: 45,
    footSize: 45
}

// stringify - делает JS объект в JSON. 

// parse - делает JSON в JS объект.

const jsonString = JSON.stringify(user);